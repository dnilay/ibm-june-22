server:
  port: 8081
spring:
  application:
    name: item-ws
  datasource:
    url: jdbc:h2:mem:testDB
    username: sa
    password: sa
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  main:
    allow-circular-references: true