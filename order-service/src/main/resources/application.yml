spring:
  application:
    name: order-ws
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8081
  
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'